import  React from  'react';

const SelectCitation = ({select}) => (
        <div>
            <button onClick={select}>Select a Simpsons citation</button>
        </div>
        
);

export  default  SelectCitation;